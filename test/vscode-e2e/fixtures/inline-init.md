# Inline init smoke test

This document verifies task 38: the editor boots from an inlined `#vmark-init`
payload instead of waiting for the `ready` → `init` host roundtrip.

- alpha
- beta
- gamma

Some **bold** text and a unique marker: INLINEINITMARKER42.
