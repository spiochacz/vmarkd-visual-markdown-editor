# Real-webview fixture

A paragraph before the code so the block is not the first node.

```js
function greet(name) {
  const msg = `Hello, ${name}!`
  return msg.toUpperCase()
}
```

> [!NOTE]
> A callout, to exercise the dual-node render in the real webview.

Trailing paragraph.
